import { Footer, Header } from "../components";
import { layout } from "../styles";
const Contact = () => {
  return (
    <section>
      <Header />
      <Footer />
    </section>
  );
};

export default Contact;
